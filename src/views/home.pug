<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet", href="/styles/styles.css")
        title 그림판
    body
        .canvasContainer
            canvas.canvas#jsCanvas
            .controls#controls
                .controls__range
                    input#jsRange(type="range" min="0.1" max="10.0" value = "5.0" step="0.1")
                .controls__btns
                    button#jsMode 채우기
                    button#jsSave 저장 
                .controls__colors#jsColors
                    div(style="background-color: #2c2c2c").controls__color#jsColor
                    div(style="background-color: white").controls__color#jsColor
                    div(style="background-color: #FF3B30").controls__color#jsColor
                    div(style="background-color: #FF9500").controls__color#jsColor
                    div(style="background-color: #FFCC00").controls__color#jsColor
                    div(style="background-color: #4CD963").controls__color#jsColor
                    div(style="background-color: #5AC8FA").controls__color#jsColor
                    div(style="background-color: #0579FF").controls__color#jsColor
                    div(style="background-color: #5856D6").controls__color#jsColor
        .loginBox
            form#jsLogin
                input(placeholder="닉네임을 적어주세요", type="text")
        .gameContainer#jsGame
            .playerBoard#jsPBoard
            .chat
                ul(style="overflow:auto").chat__messages#jsMessages
                form.chat__form#jsSendMsg
                    input(placeholder="메세지", type="text")
        script.
            window.events = !{events}
        script(src="/socket.io/socket.io.js")
        script(src="/js/main.js")